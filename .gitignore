# dependencies
node_modules
.pnp
.pnp.js

# build outputs
dist
dist-ssr
dev-dist
*.local
*.tsbuildinfo

# claude code launch config (local-only)
.claude/

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# testing
coverage
.nyc_output

# env
.env
.env.local
.env.*.local

# Strong Curves source spreadsheet (and any other .xlsx) — ignored
# by default; the checked-in artifact is the generated TS in
# src/seed/strongCurves.ts. Build with `pnpm seed:build`.
*.xlsx
scripts/source/*.xlsx
